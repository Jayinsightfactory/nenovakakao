# 다른 세션은 이 문서부터 읽으세요

2026-09-14 기준 네노바 카카오 매크로의 실행 코드·운영 상태·남은 작업 인수인계입니다.
저장소: https://github.com/Jayinsightfactory/nenovakakao (main).
이 문서의 운영 상태는 기록 시점의 스냅샷입니다. 후속 작업 전 실제 프로세스·로그를 다시 확인하세요.

## 담당자 알림·최신 원문 정책 (09-14 추가 수정)

### 쉼표로 묶은 답변 해석 수정 (09-14 16:25 이후)

`가나, 가다 보내`를 무시하던 항목 답변 파서를 수정했습니다. 쉼표(전각 쉼표 포함)로 묶은 표시들에 공통 `보내`/`안보내`를 적용합니다. 개별 지시와 섞어 쓸 수 있고, 중복 표시·충돌 지시·금지 표현의 잔여 문장은 승인하지 않습니다. 실제 저장된 요청 `4DCF43A40D54`의 답변을 두 항목 승인으로 읽는 것을 검증했습니다. **261개 테스트 통과**. 실제 전송 결과는 운영 로그에서 확인하세요.

### 업무 흐름 보완 (09-14 16시, 이 절의 정책이 이전 이력보다 우선)

- 신규 질문은 `approval_unique_labels=true`로 요청마다 겹치지 않는 영구 한글 항목 표시(`item_labels`)를 배정합니다. 모든 과거 요청의 기존 표시도 예약하며 재사용하지 않습니다. 새 질문이 끼어도 `다 보내 라 안보내`처럼 다른 요청의 고유 표시를 한 답변에 쓸 수 있습니다. 기존 발송 질문은 재표시/재발송하지 않습니다. 기존 질문의 짧은 답변은 원래 마지막 질문 경계 규칙을 유지합니다. 전송 불명 복구 건은 명시적 요청번호가 필요합니다.
- 원문 시각을 승인 질문에 표시합니다. `approval_max_source_age_sec=0`으로 신규 질문의 10분 자동 제외를 해제했습니다. 이미 별도 검토로 보존한 기록은 자동 복원하지 않습니다. 검증된 발송 질문은 자정 이후에도 답변 대기를 유지합니다. 아직 질문하지 않은 과거 날짜 원문은 기존 별도 검토 정책을 유지합니다.
- 친구 탭 진입·검색·결과 열기·유일한 전경 제목 확인 전체를 최대 2회 시도합니다. 일시정지는 즉시 전파하며, 메시지 입력 이후의 전송은 재시도하지 않습니다. 질문 전 읽기 실패는 미전송 대기열에 보존하고 15초 이후 재확인합니다.
- 전송 성공·중복 제외·승인 거절만 처리 완료입니다. 전송 불명 등은 `delivery_held`로 분리하며 새 질문을 막거나 자동 재전송하지 않습니다. 운영 콘솔은 `core.approval_progress.snapshot()`으로 요청·항목·원문 시각·답변·현장 결과·완료 알림을 연결해 표시합니다. 알림까지 확인된 항목만 화면에서 `완료`입니다.
- 검증: **253개 테스트 통과**. 여러 질문의 늦은 항목별 승인/거절 → 현장 중복 대조 → 실제 전송을 모사한 후 재조회 → 완료 알림 → 같은 기록 재처리 시 재전송 없음; 전송 불명은 완료 처리/재전송 없음; 자정·부분 답변 보존·검색 포커스 재시도를 포함합니다. 카카오 실사용 장기 검증과는 구분해야 합니다.
- 백업: `data/maintenance_backups/20260914_161638_workflow/`. 원문과 운영 상태는 Git 제외입니다. 의미가 같은 재작성/분할 메시지의 자동 중복 판단은 아직 구현하지 않았으며, 기존 본문 대조를 유지합니다.

### 15분 이후 마지막 승인 질문 답변 누락 수정

`awaiting_late_reply` 상태로 넘어가면 마지막 질문에도 요청번호 없는 `가 보내`를 버리던 문제를 수정했습니다. 정상 전송 확인된 마지막 질문은 시간 경과 후에도 항목별 답변을 처리합니다. 대화 중 새 승인 질문이 등장하면 그 이후 짧은 답변을 이전 질문에 적용하지 않습니다. 전송 불명에서 복구한 요청과 역사 검토 요청은 여전히 명시적 요청번호가 필요합니다. 현장 전달 직전 중복 대조와 전송 확인, 완료 알림은 그대로 적용합니다. 격리 테스트 **248개 통과**.

### 전달 지연 개선 (09-14 13시 후속 수정)

수입방 시트 검토 모드에서는 수집기가 원문을 `data/order_analysis_queue/`에 먼저 저장하고 체크포인트를 진행합니다. AI 분석·마스터 조회는 단일 소비자 잠금을 가진 별도 프로세스 `core.order_analysis_queue`가 수행합니다. 실패/일시정지 시 미처리 원문을 보존하고, 이벤트 ID로 중복 분석 결과 생성을 방지합니다. 카카오 UI는 기존 단일 워커만 조작합니다. 분석 로그는 `data/order_analysis.log`, 분석 완료 후 기존 검토 시트를 갱신합니다. 큐와 로그는 Git 제외입니다.

최우선 순회 간격은 처리 종료 후가 아닌 시작 시각 기준입니다. 운영 `approval_question_gap_sec`는 **5초**로 변경했습니다. `approval_dedup_at_dispatch_only=true`로 영업방 수집은 로컬 요청만 준비하고 승인 질문 직전에 추가취소방을 대조합니다. 이미 있는 내용은 `resolved_existing`으로 끝나며 담당자에게 묻지 않습니다. 승인 후 현장 전송 직전 대조와 전송 후 확인은 유지합니다. 친구 검색은 결과 렌더 지연에 대비해 메시지 입력 전 탐색만 최대 1회 재시도합니다. 전송 불명 재전송은 금지입니다.

격리 복사본에서 **243개 테스트 통과**. 기존 설정 백업: `data/maintenance_backups/20260914_132152_latency/`. 아래 예전 시각의 운영 상태와 60초 질문 간격 설명은 당시 이력이며 이 절이 우선합니다.

실행 검증에서 원문 MOYI 개별 업로드도 수집 함수에 남아 있음을 확인하여 추가 분리했습니다. `defer_archive=True`는 `data/inbound_archive_queue/`에 원문/첨부 참조를 저장한 뒤 체크포인트를 진행하며, `core.inbound_archive_queue`가 별도 프로세스에서 60초 간격으로 최대 50건을 업로드합니다. 인증값은 큐에 저장하지 않습니다. 서버 전송 결과 불명/중단은 `unknown`/`attempting`으로 보존하고 자동 재전송하지 않습니다. 해당 큐는 카카오 전송 큐가 아닙니다. 필요 시 서버 원문 대조 후 별도 복구해야 합니다. 상태 `sent`는 서버 응답 성공 뒤 저장합니다. 로그 `data/inbound_archive.log`. 수집 UI와 사진/파일 다운로드는 여전히 직렬이며 외부 UI 작업 중 선점하지 않습니다. 추가 검증 포함 **245개 테스트 통과**.

담당자에게 내부 오류 상세나 일반 상황 보고를 보내지 않습니다. `workflow_config.json`의 `operator_reports_enabled=false`가 적용되어 승인 질문과 항목 처리 완료 알림만 카톡으로 보냅니다. 나머지 알림은 `local_only` 상태로 보존합니다. 기존 대기 내부 알림 30건도 로컬 보존으로 전환했습니다.

처리 순서는 영업방 확인 → 추가취소방 실제 대화 재조회·동일 원문 제외 → 담당자 승인 질문 → 답변 반영 → 추가취소방 재확인 후 전달입니다. `require_target_history=true`이므로 대상 대화를 읽지 못하면 질문을 보내지 않습니다.

`approval_max_source_age_sec=600`: 미전송 승인 질문의 원문 시각이 10분을 넘거나 불명이면 `stale_review`로 보존하며 자동 질문하지 않습니다. 기존 미전송 2묶음이 이 상태로 이동했습니다. 이미 발송된 질문의 명시적 답변 및 전송 불명 기록은 변경하지 않습니다.

`approval_question_gap_sec=60`: 미응답 하나로 새 요청이 15분 밀리지 않도록 신규 질문 간격을 1분으로 설정했습니다. 새 질문 이후 이전 질문에 대한 답변은 요청번호를 요구하는 기존 문맥 구분을 유지합니다. 테스트 238개 통과.

D6D4FC9046B0은 10:31 원문에 대해 10:38 승인 전송 시도 중 실패한 옛 기록이며 담당자 이름이 현우였던 시점의 요청입니다. 사용자에게 뒤늦게 전달된 상세는 이 오류의 대기 알림입니다. 새로운 11시 요청으로 재전송한 것으로 해석하지 마세요.

## 최신 실행 순서와 주문 검토 시트 (09-14)

사용자 변경 요청에 따라 영업방 수집 → 승인 답변/완료 알림을 최우선 쌍으로 반복합니다. 쌍이 끝난 뒤 약 5초 후 다시 실행 대상이 됩니다. 신규 승인 후보는 원문 시각이 최신인 순서로 선택하며, 원문 보관 체크포인트는 기존 순서를 유지합니다. UI 작업은 직렬이며 진행 중인 대화 내보내기를 강제로 중단하지 않습니다.

3순위 수입방 수집·주문 분석은 약 60초 간격입니다. 운영 설정 `data/workflow_config.json`의 `order_review_only=true`로 **우선 시트 검토 모드**를 적용했습니다. 이 모드에서는 수입방 작성자를 시트 분석 대상으로 수집하고, 신규 카톡 검증/담당자 확인/등록 단계는 실행하지 않습니다. 기존 처리 상태는 보존합니다. 분석 실패도 원문과 함께 기록합니다. 담당자 카톡 확인까지 계속하라는 후속 지시가 있으면 이 설정을 false로 바꿉니다.

[수입방 주문검토 시트](https://docs.google.com/spreadsheets/d/1pXLVZqiMwWt6Vh0IhWwASBvgLtZqLnbHXMWqOLNwAXU/edit#gid=245436831)에 기존 131주문/229항목을 저장하고 전량 대조했습니다. A:R은 프로그램 갱신, 노란색 S:V는 사용자 검토 결과·거래처 수정·품목 수정·담당자/처리방식 의견입니다. 자동 갱신은 사용자 열을 쓰지 않고, 정렬 후에도 요청번호+항목 키로 행을 찾습니다. 의견은 기록용이며 주문/매칭 규칙에 자동 적용하지 않습니다.

시트 동기화는 수입방 처리 후 별도 프로세스 `scripts/sync_order_review.py`로 실행하여 카카오 화면 작업을 막지 않습니다. 상태는 `data/order_review_sync_status.json`, 로그는 `data/order_review_sync.log`. 모의 테스트에서는 이 외부 실행을 차단합니다. 구글 시트 기존 30분 분류 동기화와 별개입니다.

일반 보고·서버 전송 대기 조회·기타 방 수집·원문 보관은 30분 간격입니다. 기타 방 순회 사이에도 최우선 쌍을 실행합니다. 완료 알림은 30분 보고와 분리해 승인 처리 직후 유지합니다. 테스트 **235개 통과**. 워커/콘솔 최신 코드 로드 후 **일시정지 상태 유지**입니다. 직전 재개 검증의 보고용 대화 사전 확인 실패는 실제 재개/발송으로 재검증하지 않았습니다.

## 친구 목록 전용 1:1 경로 (09-14 후속 수정)

사용자 제공 친구 목록 화면에 따라 현재 담당자 이름은 **강현우**입니다. 보고·승인용 1:1 및 직원 개인방은 항상 친구 탭 → 이름 검색 → 친구 결과 열기를 거칩니다. 대화 목록 검색을 사용하지 않으며 같은 제목의 기존 창도 바로 재사용하지 않습니다. 친구 결과를 연 뒤 정확한 제목의 유일한 창이 실제 전경에 있는지 검증합니다. 실패하면 중단하며 채팅 탭/기존 그룹 창으로 우회하지 않습니다.

이전 담당자 설정 변경 시 친구 목록 분기에서 고정된 APPROVER를 참조하던 누락을 동적 설정으로 수정했습니다. 모의 테스트 **228개 통과**, 워커/콘솔 최신 코드 재로드, **일시정지 유지**. 실제 카카오 메시지를 테스트 발송하지 않았습니다.

## 보고·승인 담당자 변경 (09-14)

사용자 지정의 정확한 1:1 카톡 이름 **현우**로 설정했습니다. 실행 콘솔 상단의 **보고·승인 담당자 카톡 이름 → 담당자 저장**으로 변경할 수 있습니다. 완료·오류·상황 보고, 신규 전달 승인, 신규 주문 검증 질문이 `core/operator_settings.py`의 공통 설정을 읽습니다. 설정 파일은 로컬 `data/operator_config.json`이며 Git에 올리지 않습니다. 새 설치에서는 콘솔에서 담당자를 지정하세요.

저장 시 일시정지하며 사용자가 재개합니다. 현재 워커와 콘솔은 최신 코드로 재시작했고 **일시정지 유지**입니다. 실제 메시지 발송으로 검증하지 않았습니다. 과거 임재용대리에게 보낸 승인 요청은 다음 처리 시 `operator_changed_review`로 보존하고 현우의 답변과 혼용하지 않습니다. 이미 발송/결과 불명인 알림은 새 담당자에게 자동 재발송하지 않습니다. 담당자별 실제 주문 검토 방/계정 매핑은 별도 설정입니다.

현우 신규 승인·현우 답변 인식·다른 사람 답변 거부·완료 알림·주문 검증 대상·화면 이름 저장을 포함한 격리 테스트 **225개 통과**. 변경 전 기록은 로컬 `data/operator_change_backup_20260914/`에 보존했습니다.

## 수입방·견적방 대조 결과 (09-14 후속 확인)

[상세 대조 보고서](docs/sync-reconciliation-2026-09-14.md)를 확인하세요. 최신 원문을 현행 분류 방식으로 비교한 결과 네 탭에 정확한 내용 일치가 없는 후보는 수입방 7,649건, 견적방 393건입니다. 수입방 391건은 내용은 일치하나 날짜를 확정하지 못했습니다. 수입방 이벤트로그에는 동일 ID·발신자·본문의 반복 초과 행 14,651개와 한 ID의 서로 다른 본문 충돌이 있습니다. 기존 날짜 정보/현재 원문 날짜 처리 문제 때문에 기준점을 임의로 이동하지 않았습니다. 이번 후속 요청은 읽기 대조만 수행했으며 업로드/삭제/체크포인트 변경은 하지 않았습니다.

## 2026-09-14 개선 및 실제 복구

- 카카오 워커는 **일시정지 유지** 상태에서 수정 코드를 로드했습니다. 이번 작업에서 실제 카카오 메시지를 보내거나 ERP를 등록하지 않았습니다.
- 5분 개별 재안내는 기본 해제(`approval_individual_reminders=false`). 15분 미응답 상태 전환은 유지하되, 안내는 업무 시간에 최대 14요청씩 한 시간에 한 번까지 묶습니다. 이미 안내한 동일 요청은 반복하지 않으며, 과거 검토/해결 상태의 대기 알림은 발송 대상에서 제외합니다. 완료 알림과 실제 오류가 우선입니다.
- `OperationPaused`로 정상 정지를 구분합니다. 승인 사전 확인에서 정지하면 대기 상태를 유지하고, 주문 처리를 임의의 오류 보류로 바꾸지 않습니다. 가능한 외부 쓰기의 `request_unknown` 등은 그대로 보존합니다. 조정기 로그는 `outcome=paused`, 오류 횟수는 증가하지 않습니다. 실제 주문 오류에는 단계와 원인을 포함합니다.
- 과거 검토 요약은 승인 원장에서 자동 갱신합니다. 09-14 확인 결과 **71묶음/134항목**이며, 원문/답변/항목 순서를 변경하지 않았습니다.
- 시트 `메시지분류`의 K:Z 전체 55,393행을 검사해 값·메모·검증 규칙 등 콘텐츠가 없음을 확인하고 26열을 10열로 줄였습니다. 원문을 삭제하지 않고 **886,288셀** 확보.
- 보류 작업 **17,557건 / 44,492행**을 고정 ID와 배치 체크포인트로 재개하여 09:19:43 저장 완료. 네 탭의 저장 행 전체를 보류 자료와 재조회 대조해 일치 확인. `sync_pending_job.json`은 정상 완료 후 제거되었고 `sync_state.json`이 갱신됐습니다.
- 실제 할당 **9,546,738셀**, 여유 **453,262셀**입니다. 장기 용량 분할은 여전히 필요합니다. 유지보수 동안만 `NenovaSync30min`을 비활성화했으며 복구 후 Ready 상태로 되돌렸습니다.
- 복구 근거/원본 보류 작업 백업은 별도 동기화 폴더의 `data/improvement_backup_20260914/`, 전량 대조 결과는 `data/sheet_recovery_verified_20260914.json`에 있습니다. 운영 데이터이므로 Git에는 올리지 않습니다.

## 1. 실제 실행 위치

| 역할 | 이 PC의 폴더/실행 경로 |
|---|---|
| 현재 카카오 수집·승인·전달 워커 | `C:/Users/USER/Downloads/nenovakakao` |
| 운영 콘솔 | 위 폴더의 `moyi_console.py` |
| 워커 | 위 폴더에서 `python -u main.py moyi-worker` |
| 시작 점검 | `python scripts/start_supervised.py --seconds 180` — 실제 전송 가능한 워커를 재개함 |
| 별도 30분 시트 동기화 | `C:/Users/USER/nenova_agent/scripts/incremental_sync.py` |
| Windows 예약 작업 | `NenovaSync30min`, Python312의 `pythonw.exe -X utf8`로 위 스크립트 실행, 작업 폴더 `C:/Users/USER/nenova_agent` |
| 대화 내보내기 원본 | `C:/Users/USER/Downloads/카톡대화데이터` |

`Documents/nenovakakao` 등 다른 복사본을 실행본으로 오인하지 마세요. 같은 저장소를 가리켜도 폴더별 체크아웃 상태는 다릅니다.
이번 커밋의 `scripts/incremental_sync.py`와 `tests/test_sync_recovery.py`는 별도 실행 폴더에서 검증한 수정본을 이 저장소에 모은 것입니다.
후속 시트 코드 배포는 예약 작업이 사용하는 별도 폴더에도 명시적으로 반영해야 합니다. 다른 폴더의 미커밋 변경을 통째로 덮어쓰지 마세요.

09-10 수정본은 재시작 후 180초 동안 approval/sales/order 모두 성공을 확인했습니다. 09-14 수정본은 별도 환경 테스트와 일시정지 로드를 검증했으며 실제 전송 운전은 하지 않았습니다.
커밋 준비 시점에는 2026-09-10 14:37:23(KST) Ctrl+Alt+F10 긴급 정지 기록 이후 `paused=True`, 워커 없음으로 확인했습니다. 이번 커밋 작업에서는 재시작하지 않았습니다.
현재 정지 여부는 `core.moyi_control.is_paused()`와 `worker_processes()`로 다시 확인합니다.
긴급 정지는 운영 콘솔의 **Ctrl+Alt+F10**입니다. 콘솔을 열어 두어야 작동합니다.
새 세션에서 이 문서를 읽었다는 이유만으로 워커를 중복 실행하거나 일시정지를 해제하지 마세요. 사용자의 현재 작업 요청에 따라 진행합니다.

## 2. 대화 업로드 구조와 GitHub 경로

1. **원문 보관/분석**: `core/moyi_inbound.py` → `core/mindmap_sink.py` → mindmap-viewer의 `POST /api/kakao/import`.
   - 기본 서버: `https://mindmap-viewer-production-adb2.up.railway.app`.
   - 전송 필드: 원문, 방 이름, 작성자, 원문 시각, 고유 메시지 ID.
   - 서버 저장 테이블: PostgreSQL `kakao_messages`.
   - 수신 코드: https://github.com/Jayinsightfactory/mindmap-viewer/blob/main/routes/kakao-decrypt.js
   - 분석 코드: https://github.com/Jayinsightfactory/mindmap-viewer/blob/main/bin/kakao-intel-worker.js
   - 설계 배경: https://github.com/Jayinsightfactory/mindmap-viewer/blob/main/docs/HANDOFF_KAKAO_FULL_CAPTURE.md (과거 진행 상태는 현행과 다를 수 있음).
2. **MOYI 대화방 연동**: `core/moyi_inbound.py` → `https://api.nowlink.kr/kakao/agent/inbound` → MOYI 메시지 DB.
   - 수신 코드: https://github.com/jay-sinsight/talkhub/blob/main/backend/app/api/kakao_connect.py
   - 첨부파일 업로드와 원문 텍스트 보관은 별도 경로입니다. 일부 첨부는 확보 실패 시 보류하며 후속 텍스트 수집을 계속합니다.
3. **구글시트 분류 동기화**: `scripts/incremental_sync.py` → 이벤트로그/비즈니스이벤트/의사결정추적/메시지분류 탭.
   - 09-14 용량 확보 후 기존 보류 작업 저장 완료. 카카오 워커와는 별개의 30분 예약 작업입니다.

실제 대화 데이터는 GitHub 업로드 대상이 아닙니다. 코드와 이 인수인계 문서를 커밋합니다.
`SOLUTION_LINK.md` 등의 예전 개발 진행 상태보다 현재 코드와 실제 로그를 우선 확인하세요.

## 3. 승인/전달 규칙

- 추가·취소·변경 후보는 임재용대리의 승인 후 현장 추가취소방에 전달합니다.
- 원문 항목은 가·나·다…로 표시합니다. 답변 예: **가 보내 나 안보내**. 한 건씩 따로 답변해도 됩니다.
- 미응답 항목은 대기합니다. 부분 답변으로 다른 항목을 자동 승인/거절하지 않습니다.
- 오래된 요청 또는 다음 질문 이후에는 요청번호를 함께 적어야 합니다. 예: `요청번호 가 안보내`.
- `안보내` 처리 후 완료 알림, `보내`는 실제 전달 확인 후 완료 알림을 보냅니다. 동일 요청에서 처리된 항목은 묶어 우선 발송합니다.
- 대상 방 동일 원문을 재확인하며, 전송 결과가 불명확하면 자동 재전송하지 않습니다.
- 현행 로컬 설정: `approval_current_day_only=true`, `approval_active_hours=[8,19]`. 새 질문은 오늘 원문·08~19시 기준, 이미 온 답변 처리는 시간과 무관합니다. 이는 별도 선호 답변이 없어 적용한 운영 기본값이며 사용자 지시로 변경할 수 있습니다.
- 09-10 기준 과거 요청 31묶음/88항목은 `historical_review`로 보존했습니다. 자동 재질문하지 않으며 ID·항목 순서·기존 답변을 보존합니다. 원문이 섞인 묶음은 재번호 부여 없이 전체 별도 검토로 둡니다.
- ERP는 내용 확인과 실제 등록 요청을 분리합니다. **실제 등록 어댑터는 검증 미완료로 `core/order_services.py::register_bulk`에서 차단 중입니다.** 추정 API로 등록을 다시 켜지 마세요.

## 4. 이번 수정의 근거와 구현

- 일반 수집 보고가 `sent/unknown`에서 `queued`로 되돌아가 같은 ID로 반복 발송되던 버그 수정. 시간 단위 집계가 끝난 후 한 번 발송.
- 완료 알림을 같은 요청 최대 14항목씩 묶고 승인 처리 직후 실행. 승인 에이전트를 수집보다 우선 실행.
- 일부 항목 답변 후에도 미응답 타이머를 진행하고, 완료 항목 상태를 보존하며 늦은 승인/거절 처리 복구.
- 원문 날짜별 과거 요청 분리, 콘솔의 과거 검토 수 및 새 답변 문구 반영.
- 예약 작업을 CMD 배치 실행에서 pythonw 직접 실행으로 변경해 화면 포커스 간섭 완화. 예약 작업 설정은 Git이 관리하지 않으므로 다른 PC에서는 별도로 설치/확인해야 함.
- 시트 저장 실패를 성공으로 기록하던 로그 수정, 오류 종료코드 반환, 고정된 미저장 작업/배치별 체크포인트 저장, 결과 불명 재전송 차단, RAW 값 저장.
- 동기화 기준 MD5가 사라지면 전체 대화를 재업로드하지 않고 원문 대조를 위해 보류.
- `overdue_ms` 계측 및 시작 점검을 180초로 확대. 시작 점검은 장시간 안정성 보장을 의미하지 않습니다.

## 5. 남은 작업 — 우선순위

1. **시트 장기 용량 분할과 기준점 대조**: 09-14 기존 보류 17,557건 저장은 완료됐지만 여유는 453,262셀입니다. 별도 문서/기간별 저장 구조를 준비해야 합니다.
   - 향후 `capacity_blocked`/`unknown`이 생기면 작업 파일을 지우거나 확인 없이 상태를 풀지 마세요. 실제 용량/전송 결과를 검증하고 고정 작업·체크포인트를 기준으로 재개합니다.
   - 수입방/견적방은 동기화 기준점을 못 찾은 별도 원문 대조 작업이 남아 있습니다. 이번 용량 복구가 이 문제까지 해결했다는 뜻은 아닙니다.
2. **운영 장시간 관찰**: 실제 답변→처리→알림 시간, 반복 알림, 30분 예약 작업과 UI 간섭, 과거 요청 보존, 부분/늦은 답변을 점검.
3. **실제 ERP 등록**: 검증된 API·권한·단위·중복 방지·저장 후 정확한 재조회가 준비될 때까지 기존 차단 유지.

## 6. 로컬 로그/상태 — Git에 새로 올리지 않음

현재 실행 폴더의 `data/` 아래:
- `moyi_control_events.jsonl`: 시작/정지/알림 전송 결과
- `agent_runtime_events.jsonl`, `moyi_events.jsonl`: 처리 시간과 오류
- `keyword_approval_requests.json`, `keyword_forward_state.json`: 승인·전달 상태
- `error_notifications.json`: 완료 알림/오류/상황 보고 상태
- `historical_approval_review.json`: 과거 요청 요약 (원본은 승인 요청 파일에 보존)
- `mindmap_kakao_outbox.json`, `moyi_inbound_state.json`: 원문 보관 대기/수집 처리 이력
- `supervised_start_result.json`: 최근 시작 점검 결과
- `maintenance_backups/20260910_093405/`: 수정 전 주요 소스·상태·예약 작업 XML

별도 동기화 폴더: `data/sync_pending_job.json`, `data/sync_state.json`, `logs/sync.log`.
토큰은 로컬 `.env`/보안 파일·자격 증명 저장소에서 사용합니다. 값을 출력하거나 커밋하지 마세요.
이 저장소에는 과거부터 추적된 seed/config 파일이 일부 있습니다. 이번 커밋은 새 운영 데이터·원문·백업을 추가하지 않습니다.

## 7. 검증과 후속 작업 시작

이번 게시 대상 파일을 운영 데이터·토큰이 없는 별도 체크아웃에 복사한 뒤 `python -m pytest tests -q` 실행: **215개 통과**.

1. 이 문서를 읽고 사용자 요청의 대상이 수집/승인/원문 보관/시트 중 무엇인지 확인.
2. `git status`, 실제 실행 폴더, 프로세스·정지 상태, 최근 로그 확인. 다른 복사본의 변경을 덮어쓰지 않기.
3. 일반 검증: Windows/Python 환경에서 `python -m pytest tests -q`. 테스트는 API/화면 조작을 모의하고 임시 상태를 사용하며, 깨끗한 체크아웃에서 실행하는 것을 권장.
4. 사용자에게 실제 시작/재시작이 요청된 경우 운영 콘솔의 정지 단축키 준비를 확인한 뒤 `python scripts/start_supervised.py --seconds 180` 사용.
5. 원문·상태·토큰 없이 코드와 변경한 인수인계 내용만 커밋.

다른 세션에는 이 GitHub 문서 주소 하나와 원하는 후속 작업을 전달하면 됩니다.
